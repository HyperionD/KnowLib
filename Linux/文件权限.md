# 文件权限相关

## umask

umask控制新建文件和文件夹的默认权限，默认为0022，

此时新建文件的权限为644（新建文件不允许有x权限所以最大为6）

新建文件夹权限为755

修改默认umask值更改/etc/profile

## suid

当执行该文件时将具有该文件所有者的权限

## sgid

如果SGID设置在二进制文件上，则不论用户是谁，在执行该程序的时候，它的有效用户组（effective group）将会变成该程序的用户组所有者

如果SGID是设置在A目录上，则在该A目录内所建立的文件或目录的用户组，将会是此A目录的用户组