# box-shadow

```css
box-shadow: <offset-x> <offset-y> <blur-radius>? <spread-radius>? <color>?

/* <offset-x> 水平位移，负值则阴影在左侧
   <offset-y> 垂直位移，负值则阴影在上部
   <blur-radius> 指定模糊半径，不允许负值，为0则阴影不模糊
   <spread-radius> 指定阴影拓展，假如设置为0，不拓展，正值阴影扩大，负值缩小
*/
```

